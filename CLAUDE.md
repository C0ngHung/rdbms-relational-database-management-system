@/home/ubuntu/Data_D/learnByMyself/course/database/rdbms-relational-database-management-system/AGENTS.md

## Claude Code — bổ sung riêng

- Task đụng nhiều hơn 3 file → trình bày plan trước, chờ tôi duyệt.
- Sau khi sửa schema SQL, chạy lại các query liên quan để xác nhận không vỡ.
- Không commit / push khi tôi không yêu cầu. Luôn làm trên feature branch, không sửa trực tiếp trên `main`.
- Slash command của repo: `.claude/commands/*.md` — nội dung quy trình thật nằm ở `.agents/workflows/*.md`.
