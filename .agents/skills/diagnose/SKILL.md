---
name: diagnose
version: 1.0.0
description: SQL diagnosis workflow for unknown-cause bugs. Hypotheses and evidence first, fix only after root cause confirmed.
when_to_use: "When the user reports a database problem with unknown cause — slow query, wrong result, deadlock, constraint violation — and needs systematic investigation before any fix."
allowed-tools: Read, Edit, Glob, Grep, Bash
---

# Diagnose

Read [../../workflows/diagnose.md](../../workflows/diagnose.md) and follow that
procedure exactly, treating the user's message as its input.

That file is the single source of the procedure — do not restate or summarise it here.
