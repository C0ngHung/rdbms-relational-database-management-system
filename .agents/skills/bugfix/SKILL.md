---
name: bugfix
version: 1.0.0
description: Bug fixing workflow for SQL/database issues. CONTEXT / ROOT CAUSE / CONSTRAINTS / SUCCESS.
when_to_use: "When the user reports a wrong query result, a constraint violation, a slow query, or any SQL/schema bug in this RDBMS project."
allowed-tools: Read, Edit, Glob, Grep, Bash
---

# Bug Fix

Read [../../workflows/bugfix.md](../../workflows/bugfix.md) and follow that
procedure exactly, treating the user's message as its input.

That file is the single source of the procedure — do not restate or summarise it here.
